server:
  context-path: /BookManager
  port: 8081
spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/book
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

